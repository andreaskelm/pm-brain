# Evals (Cursor pointer)

Canonical eval infrastructure lives in [`system/evals/`](../../system/evals/README.md).

Use that folder for guides, checklists, scenarios, and eval-results. This path exists only if Cursor or hooks expect `.cursor/evals/` on disk.
