# Claude Code Commands

Canonical command bodies live in [`.cursor/commands/README.md`](../../.cursor/commands/README.md). Each file in this folder is a thin wrapper that points at the matching `.cursor/commands/*.md` file.

**Requires:** The `.cursor/commands/` tree must be present in the repo (by design — same pattern as skills canonical in `system/skills/`).
